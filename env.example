# Copy to `.env` next to sayari_mcp_client.ipynb and fill in. Do NOT commit `.env`.

# Sayari MCP client credentials (required)
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=

# LLM provider key — only needed for step 4 of the notebook.
# Match this to LLM_MODEL / LLM_API_KEY_ENV in the notebook's configuration cell.
ANTHROPIC_API_KEY=
